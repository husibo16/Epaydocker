# =========================================
# 应用基础配置
# =========================================

# 应用运行环境：
# local（本地开发）、testing（测试环境）、production（生产环境）
APP_ENV=production

# 应用的访问地址（生产环境应改为实际域名）
APP_URL=http://localhost

# 是否启用调试模式：
# true 会显示详细错误信息（仅限开发），false 隐藏错误（生产环境必须为 false）
APP_DEBUG=false
TZ=Asia/Shanghai

PHP_TIMEZONE=

# =========================================
# 数据库配置
# =========================================

# 数据库主机地址（Docker 环境中通常为服务名 mysql）
DB_HOST=mysql

# 数据库端口号（MySQL 默认 3306）
DB_PORT=3306

# MySQL 管理员 root 用户的密码
MYSQL_ROOT_PASSWORD=secret

# 应用要使用的数据库名
MYSQL_DATABASE=epay

# 应用程序连接数据库使用的用户名
MYSQL_USER=epay

# 上述用户的数据库密码
MYSQL_PASSWORD=epay


# =========================================
# Redis 配置
# =========================================

# Redis 主机地址（Docker 环境中通常为 redis）
REDIS_HOST=redis

# Redis 端口号（默认 6379）
REDIS_PORT=6379

# Redis 密码（如果未设置密码则留空）
REDIS_PASSWORD=


# =========================================
# PHP 环境配置
# =========================================

# PHP 脚本最大可用内存
PHP_MEMORY_LIMIT=256M

# PHP 上传文件的最大大小限制
PHP_UPLOAD_LIMIT=64M
