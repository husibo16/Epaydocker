# 应用环境配置
APP_ENV=production          
APP_URL=http://localhost    
APP_DEBUG=false             
TZ=Asia/Shanghai            

# PHP 相关配置
PHP_TIMEZONE=               
PHP_MEMORY_LIMIT=256M       
PHP_UPLOAD_LIMIT=64M        

# 数据库配置
DB_HOST=mysql               
DB_PORT=3306                
MYSQL_ROOT_PASSWORD=secret  
MYSQL_DATABASE=epay         
MYSQL_USER=epay             
MYSQL_PASSWORD=epay         

# Redis 缓存配置
REDIS_HOST=redis            
REDIS_PORT=6379             
REDIS_PASSWORD=             

# 任务调度配置
SCHEDULER_INTERVAL=60       


# ===========================
# 以下为参考说明模板（不会被读取）
# ===========================
# 应用环境配置
#APP_ENV=production          # 应用运行环境（production=生产环境, local=本地, staging=测试）
#APP_URL=http://localhost    # 应用访问的基础 URL（生产环境应改为你的域名）
#APP_DEBUG=false             # 是否启用调试模式（生产环境务必为 false）
#TZ=Asia/Shanghai            # 系统时区设置（此处为中国标准时间）
# PHP 相关配置
#PHP_TIMEZONE=               # PHP 内部时区（若留空，将使用系统 TZ 设置）
#PHP_MEMORY_LIMIT=256M       # PHP 最大内存限制
#PHP_UPLOAD_LIMIT=64M        # PHP 文件上传大小限制（例如 64M 表示最大允许上传 64MB 文件）
# 数据库配置
#DB_HOST=mysql               # 数据库主机地址（通常与 docker-compose 服务名一致）
#DB_PORT=3306                # 数据库端口
#MYSQL_ROOT_PASSWORD=secret  # MySQL root 用户密码（生产环境请使用强密码）
#MYSQL_DATABASE=epay         # 默认数据库名
#MYSQL_USER=epay             # 应用数据库用户名
#MYSQL_PASSWORD=epay         # 应用数据库用户密码
# Redis 缓存配置
#REDIS_HOST=redis            # Redis 服务主机地址（通常与 docker-compose 服务名一致）
#REDIS_PORT=6379             # Redis 端口
#REDIS_PASSWORD=             # Redis 密码（如果设置了 requirepass，需要填写）
# 任务调度配置
#SCHEDULER_INTERVAL=60       # 调度器运行间隔（单位：秒）
